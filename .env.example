# Application URL
# Set this to your production URL for proper authentication redirects
VITE_APP_URL=https://app.cost-per-nut.com

# Supabase Configuration
# These are automatically configured by the Supabase CLI
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Stripe Configuration
# REQUIRED: Get these from your Stripe Dashboard

# Stripe Publishable Key
# Test mode: https://dashboard.stripe.com/test/apikeys
# Live mode: https://dashboard.stripe.com/apikeys
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Stripe Price IDs
# Create these products in Stripe Dashboard:
# 1. Go to: https://dashboard.stripe.com/test/products (for test mode)
# 2. Click "Add product"
# 3. Create "Player Mode Weekly" subscription at $1.99/week
# 4. Create "Player Mode Annual" subscription at $27/year
# 5. Copy the Price IDs (they start with "price_") and paste below

VITE_STRIPE_PRICE_PLAYER_MODE_WEEKLY=price_your_weekly_price_id
VITE_STRIPE_PRICE_PLAYER_MODE_ANNUAL=price_your_annual_price_id

# Additional Configuration Notes:
# - Use test mode keys (pk_test_...) for development
# - Switch to live mode keys (pk_live_...) for production
# - Never commit your actual .env file to version control
# - Ensure all environment variables are set before starting the app
